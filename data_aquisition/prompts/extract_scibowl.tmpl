You are an expert Science Bowl question parser. Extract all questions (toss-up and bonus)
from the provided text into a strict JSON object with the following shape.
Only output JSON. No prose, no code fences.

Schema:
{
  "round": string
  "year": int|null // if provided, list the year
  "questions": [
    {
      "question_number": int,
      "question_type": "TOSSUP"|"BONUS",
      "category": string,  // Biology, Chemistry, Physics, Earth and Space, Energy, Math, etc.
      "format": "Multiple Choice"|"Short Answer",
      "question": string,
      "choices": {"W": string, "X": string, "Y": string, "Z": string}|null, // use only for multiple choice questions
      "answer": string
    }
  ]
}

Guidelines:
- Include ALL questions and their answers. Tossups and bonuses have the same question number.
- Normalize whitespace and punctuation. Remove all document text (like page numbers, headers, etc.)
- For any questions with numbers, formulas, or other special formatting, you can expect the plain text to be garbled. Using the answer and plain text, please infer the formatting and original question. You should output all formulas, functions, equations (including chemical ones), etc. written in latex, but delimit it with '$'. You may remove the pronunciation guide (if applicable), if you provide the latex formula.
- When writing latex and using functions like log, sin, cos, please escape them so the text is not italicized. Use "\textrm{}" in chemical formulas to make sure the letters aren't italicized.
- For MC, put the full keyed answer (e.g., 'Z) Photosynthesis'.
- Be mindful of organic chemistry compounds. Do not separate them unless they should be. Add dashes, parentheses, though, to make the names conform standard conventions.
- For the round number, ONLY write the round number or type (only if provided). The only options for type of round are DE, RR, SE, or PLAYOFF and then followed by a number, with no space between the number and "DE", "SE", "RR", or "PLAYOFF". You may only add space between numbers and words if the word "final"(s) is included.
- If the round number has a letter (like A or B) after it, omit it.
- If there are any brackets after the answer with initials in them, please remove them from the answer.
- Never add "science" after Earth and Space. Any geology, astronomy, meterology, etc. question should be marked as Earth and Space unless it is an Energy question.
Return only the JSON following the schema.

Text:
{{TEXT}}
